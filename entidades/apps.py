from django.apps import AppConfig

class EntidadesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'entidades'  # Asegúrate de que coincide con el nombre de la carpeta
